import random

# Generating random floats and converting to strings
str_a = str(random.random())
str_b = str(random.random())
str_c = str(random.random())

print(f"Python outputs: {str_a}, {str_b}, {str_c}")