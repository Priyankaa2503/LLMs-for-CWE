# Python has no preprocessor. The closest equivalent is the 'import'
# statement, which loads a module at runtime (not compile time).
# For basic printing, Python's built-in print() needs NO imports at all —
# it is part of the language's global built-ins, always available.

print("Hello, World!")