// JavaScript has NO preprocessor. The modern equivalent of #include
// is the ES Module 'import' statement, resolved by the JS engine at load time.
//
// console.log() is globally available in every JS environment
// (browsers expose it via window.console; Node.js exposes it globally).
// No import is needed.
//
// Equivalent of #include <stdio.h>  → not needed; console.log() is global
// Equivalent of #include <stdlib.h> → not needed for basic output

console.log("Hello, World!");