// JavaScript has no preprocessor. In modern JS (ES Modules), the
// closest equivalent is the 'import' statement. In Node.js, console.log()
// is a globally available built-in — no import needed.
// In a browser, it is also globally available via the window object.

console.log("Hello, World!");