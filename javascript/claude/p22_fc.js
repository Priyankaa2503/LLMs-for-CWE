'use strict';

/*
 * In Node.js, console.log() is a built-in global — no require() or import
 * is needed for basic printing.
 *
 * The optional require below demonstrates how a built-in module is loaded
 * when standard-library functionality beyond console is needed.
 */

// Built-in 'process' module is available globally in Node.js.
// Shown here to illustrate the require pattern; not needed just to print.
// const process = require('process'); // (already global — commented out)

console.log('Hello from JavaScript (Node.js)!');
