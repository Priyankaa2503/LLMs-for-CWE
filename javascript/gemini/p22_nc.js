// Require statement: require (CommonJS) or import (ES Modules)
// In Node.js, 'console' is a global object, so no explicit import is needed for basic printing.
// However, to demonstrate the concept, we can require the 'util' module or similar, 
// or just comment on the module system.
// Here we use the built-in console which is globally available.

// Equivalent to #include <stdio.h> in C, but for modules:
// const fs = require('fs'); // Example of importing a module

console.log("Printing functionality is available.");
