// p22: demonstrate module includes analogous to preprocessor directives
// In Node.js we 'require' modules instead of preprocessor includes
const fs = require("fs");
const os = require("os");

console.log("Modules fs and os are available");
