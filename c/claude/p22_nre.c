/* provides printf, fprintf, puts — the standard I/O header */
#include <stdio.h>

/* provides malloc, free, exit, atoi, and other general utilities */
#include <stdlib.h>

int main(void)
{
    puts("Includes loaded: stdio.h (printing) and stdlib.h (standard library).");
    return 0;
}
